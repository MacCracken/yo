# yo — main entry point
# Stdlib auto-included via cyrius.cyml

include "src/icmp.cyr"

fn main() {
    alloc_init();
    println("hello from yo");
    return 0;
}

var r = main();
syscall(SYS_EXIT, r);
